package widget;

import glyph.Button;
import glyph.Glyph;
import window.Window;

class GreenButton extends Button {

    GreenButton(Window window) {
        super(window);
        this.myColor = "Green";
    }
}
