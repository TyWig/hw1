package widget;

import glyph.Button;
import glyph.Glyph;
import window.Window;

class RedButton extends Button {

    RedButton(Window window) {
        super(window);
        this.myColor = "Red";
    }
}
