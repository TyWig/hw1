package widget;

import glyph.Glyph;
import glyph.Label;
import window.Window;

class GreenLabel extends Label {
    GreenLabel(Window window) {
        super(window);
        this.myColor = "Green";
    }
}
